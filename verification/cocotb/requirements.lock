# Pinned Python dependencies for the cocotb + Verilator verification environment.
# Installed into a venv created FROM the MSYS2 ucrt64 (MinGW) python (verification/cocotb/.venv)
# -- the venv shares the ucrt64 libpython, so the VPI link stays ABI-compatible; an MSVC-based
# python/venv does NOT work. install_toolchain.ps1 creates the venv and installs this; see
# toolchain/README.md.
#
# Pin cocotb + Verilator together: the static VPI lib (bootstrap_vpi.py) is rebuilt from
# the cocotb sources matching this exact version and linked against the installed Verilator.
cocotb==2.0.1
find_libpython==0.5.1
pytest>=8,<9
# pyuvm: real UVM in pure Python; 4.0.1 supports cocotb 2.0. Used by the lib/uvm/ base and
# pyuvm tests only; the 53 plain-lib tests do not depend on it.
pyuvm==4.0.1
